#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$ROOT"

if [[ -z "${IN_NIX_SHELL:-}" ]]; then
	exec nix develop -c bash -c './test_ape'
fi

make ape >/dev/null
exec ./tests/test.sh ./bin/xattr_stream_ape.com
