# PROJECT_PLAN

## Done Criteria

- `./test` passes on Linux and macOS.
- `make native` builds `bin/xattr_stream`.
- `make ape` builds `bin/xattr_stream_ape.com` on Linux x86_64 with `cosmocc` available.
- CLI implements `put|get|len|del|lst|limits` and `--nofollow|--help|--version`.
